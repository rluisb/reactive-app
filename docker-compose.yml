version: '3.1'
services:
  mysql:
    image: mysql:5.7
    restart: always
    environment:
        - MYSQL_DATABASE=college
        - MYSQL_ROOT_PASSWORD=root
        - MYSQL_USER=admin
        - MYSQL_PASSWORD=admin
    ports:
      - '3306:3306'
